---
contact_email: 

acme_accounts: 
  - name: 
    server: 
    mac_id: 
    # mac_id: "{{ vault_service_mac_id }}"
    mac_key: 
    # mac_key: "{{ vault_service_mac_key }}"

# acme_accounts: 
  # - name: "SERVICE NAME"
  #   server: https://acme.sectigo.com/v2/OV
    # mac_id: "{{ vault_service_mac_id }}"
    # mac_key: "{{ vault_service_mac_key }}"